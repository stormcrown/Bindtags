<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@null"
    >

    <TextView
        android:id="@+id/com_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:alpha="0.5"
        android:background="@null"
        android:text="@string/app_name"
        android:textColor="@color/light_orange_yellow"
        android:textSize="26sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/deleteIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:contentDescription="@string/delete"
        android:src="@drawable/close"
        android:tint="@color/colorAccent"
        app:layout_constraintBottom_toBottomOf="@id/com_text"
        app:layout_constraintEnd_toEndOf="@id/com_text"
        app:layout_constraintTop_toTopOf="@id/com_text" />
</android.support.constraint.ConstraintLayout>